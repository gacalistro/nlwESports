- Creates a mobile project
  => expo init mobile

- Run
  => expo start

- expo install
  => react-native-safe-area-context
  => expo-linear-gradient

- ...rest
  extract {...rest} from an interface resumes everything this interface has
